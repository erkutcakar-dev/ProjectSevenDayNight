@model List<ProjectSevenDayNight.Models.DataModels.Employee>
@{
    ViewBag.Title = "Employee List";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<br />
<br />
<br />
<br />

<div class="col-md-12">
    <div class="card">
        <div class="card-header">
            <div class="d-flex align-items-center">
                <h4 class="card-title">Çalışan Listesi</h4>
                <a href="/Employee/CreateEmployee" class="btn btn-primary btn-round ms-auto">
                    <i class="fa fa-plus"></i> Çalışan Ekle
                </a>
            </div>
        </div>

        <div class="card-body">
            <div class="table-responsive">
                <table id="add-row" class="display table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Title</th>
                            <th>Subtitle</th>
                            <th>ImageUrl</th>
                            <th>NameSurname</th>
                            <th>Job</th>
                            <th style="width: 10%">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.EmployeeId</td>
                                <td>@item.Title</td>
                                <td>@item.Subtitle</td>
                                <td>@item.ImageUrl</td>
                                <td>@item.NameSurname</td>
                                <td>@item.Job</td>
                                <td>
                                    <div class="form-button-action d-flex gap-2">
                                        <a href="/Employee/UpdateEmployee/@item.EmployeeId"
                                           class="btn btn-primary d-flex align-items-center gap-1"
                                           data-bs-toggle="tooltip"
                                           title="Edit">
                                            <i class="fa fa-edit"></i> <span>Update</span>
                                        </a>
                                        <a href="/Employee/DeleteEmployee/@item.EmployeeId"
                                           class="btn btn-danger d-flex align-items-center gap-1"
                                           data-bs-toggle="tooltip"
                                           title="Delete"
                                           onclick="return confirm('Silmek istediğinize emin misiniz?');">
                                            <i class="fa fa-times"></i> <span>Delete</span>
                                        </a>
                                    </div>

                                </td>

                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div> 